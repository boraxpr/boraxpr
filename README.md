<a href="https://github.com/boraxpr">
  <img src="header.svg" alt="Click to see the source">
</a><br>
I am driven by a fundamental need to understand how things work under the hood. I don’t like leaving unknowns on the table. If a technology exists in my orbit, I feel compelled to understand how it works. This curiosity is what allows me to connect the dots across the entire stack, from raw requirements to production reliability. Not a template user, tutorial follower – I hands-on setup, configure, deep integrate. I can setup project myself on multiple stacks. I know what each part does on each ecosystem for example; DI Containers behavior across multiple ecosystems or lack thereof – Java Spring Boot has DI container while NestJS tries to replicate that because NodeJS has none so it’s module pattern, webserver behind each stack for example Java ecosystem uses Tomcat, while node/go/rust uses its embedded server inside itself. Endpoint Validation how multiple frameworks have their own tooling of catching body, header, query. How different ecosystems use different ways to handle concurrency: NodeJS – Event Loop, Golang – Goroutine, Rust – Low level 3rd party Tokio. How each ecosystem connects to database; Java Spring Boot has autoconfiguration where you just put the config on properties and it auto creates the object (bean) for you while in NodeJS, Go, Rust you manage this by mostly using singleton pattern. I know the best practice like connection pool instead of starting the connection on every endpoint calls. I know tuning like connection pool number tuning to match database pricing tier. And probably more, that’s why I differentiate myself from other devs-engineers you might see on the market. I actually understand the why behind the code so I confidently claim that can solve problem most engineers from Thai market can’t.  
-	Understand Business not only tech. I tailor solution to fit business needs and constraint.
-	Stance on AI. I use AI hands-on to do complex tasks every day. So, I know its limitation and way to work with AI.
-	Recent Highlight: Rescued and stabilized an initially unreliable internal LLM platform—scaling the infrastructure to comfortably support 1,000+ DAU and 5,000+ registered users—while successfully navigating complex stakeholder dynamics and resetting volatile project expectations.

---

<img width="1171" height="937" alt="image" src="https://github.com/user-attachments/assets/66171ac9-1c4d-4bd8-a09e-5b4befd4e770" />

<summary>🌀 Origin of 'boraxpr'</summary>

**Bora** is from *Campagnolo Bora One*, a high-end roadbike wheel I dreamed of owning in high school.  
I ended up with *Campagnolo Scirocco*, but found its name a bit too soft-sounding for a personal alias.

**Xpr** stands for *Cross-Polarization Ratio*, a concept from antenna engineering I encountered while studying networks at MUICT.  
It refers to how much energy is radiated in unintended polarization — an idea that parallels how my mind works:  
creative, sometimes unconventional, but always grounded in signal.

</details>

